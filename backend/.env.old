# Underground Utility Detection Platform - Environment Configuration
# =================================================================

# Application Settings
APP_NAME="Underground Utility Detection Platform"
VERSION="1.0.0"
DEBUG=true
HOST=0.0.0.0
PORT=8000

# Security
SECRET_KEY=dev-secret-key-for-testing-only-change-in-production-12345
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS and Security
ALLOWED_HOSTS=["*"]
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080", "http://127.0.0.1:8000"]

# Database Configuration (SQLite for development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=gpr_app_user
DB_PASSWORD=dev_password_2024
DB_NAME=gpr_platform

# Database Connection Pool
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30

# File Storage Paths
DATA_ROOT_PATH=/Users/nitishgautam/Code/prototype/ground-truth/datasets
UPLOAD_MAX_SIZE=104857600  # 100MB

# GPR Processing Settings
SAMPLING_FREQUENCY=1000.0
TIME_WINDOW=100.0
DEPTH_CALIBRATION=0.1

# Batch Processing
BATCH_SIZE=10
MAX_WORKERS=4
PROCESSING_TIMEOUT=300

# Machine Learning
MODEL_CACHE_SIZE=100
FEATURE_VECTOR_SIZE=512
ML_CONFIDENCE_THRESHOLD=0.8

# Logging Configuration
LOG_LEVEL=INFO
LOG_ROTATION=10 MB
LOG_RETENTION=1 week

# Optional: Redis for caching
# REDIS_URL=redis://localhost:6379/0
# CACHE_TTL=3600

# Monitoring
ENABLE_METRICS=true
METRICS_PORT=9090